---
phase: 7
plan: 3
wave: 2
---

# Plan 7.3: Intent Playground (MCP Tool)

## Objective
Give developers an instant feedback loop for testing the Ollama intent generation pipeline: type in a raw gesture shorthand, optionally provide classroom context, and immediately see the expanded fluent sentence — all without running the full Hub stack.

## Context
- hub/services/intent_service.py
- hub_services MCP Server (if available)

## Tasks

<task type="auto">
  <name>Create `scripts/intent_playground.py` CLI</name>
  <files>scripts/intent_playground.py</files>
  <action>
    - Create a standalone script at `scripts/intent_playground.py`.
    - Import `generate_fluent_speech` from `hub.services.intent_service`.
    - Enter a REPL loop: prompt the user for `shorthand` (e.g., `question`) and optional `context` (defaults to "The teacher is explaining photosynthesis.").
    - Print the generated output immediately.
    - Also implement this as an MCP Tool in the `hub_services` server if one exists.
  </action>
  <verify>python scripts/intent_playground.py</verify>
  <done>Script prints Ollama output for any given shorthand + context pair.</done>
</task>

## Success Criteria
- [ ] Any prompt engineer on the team can iterate on the Ollama prompt without touching the full Hub pipeline.
